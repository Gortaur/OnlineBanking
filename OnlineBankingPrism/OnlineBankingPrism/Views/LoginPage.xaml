<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="OnlineBankingPrism.Views.LoginPage">
    <ContentPage.Content >
        <StackLayout Margin="50" >
            <Label VerticalOptions="Center" Text="Username" />
            <Entry Text="{Binding Login}" VerticalOptions="Center" x:Name="UsernameEntry" Placeholder="username" />
            <Label VerticalOptions="Center" Text="Password" />
            <Entry Text="{Binding Password}" VerticalOptions="Center" x:Name="PasswordEntry" IsPassword="true" />
            <Button VerticalOptions="Center" Text="Login" Command="{Binding SignInCommand}" />
            <Label VerticalOptions="Center" x:Name="MessageLabel" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>